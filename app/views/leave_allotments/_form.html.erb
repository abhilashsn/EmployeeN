
<div class = "container">



<%#= form_for([@todo_list, @todo_list.todo_items.build]) do |f| %>

<%= form_for(@leave_allotment) do |s|%>



	<div class="form-group">
	<label>Select employee:</label>
	<%= s.collection_select :employee_id ,Employee.all , :id , :name%>
     <%#= s.collection_select :number_id, Number.all, :id , :number, prompt: "select state"%>
 	
 	
	<label>Select leave structure:</label>
	<%= s.collection_select :leave_structure_id ,LeaveStructure.all , :id , :name%>
     <%#= s.collection_select :number_id, Number.all, :id , :number, prompt: "select state"%>
    </div>

	<div class="form-group ">

	<%= button_tag(type: 'submit', class: "btn btn-primary") do %>
 	<i class="icon-ok icon-white"></i> Save changes
	<% end %>
	</div>

<%end%>
</div>


